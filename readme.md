# n e o v i m

### TODOLIST
- Terminal command sending, if a command is sent and the terminal is not running a command then send the command, otherwise just change buf. 
- Terminal toggle things. Send code to split terms, toggle splits open/close.

